{
  "name": "@repo/api",
  "version": "4.5.0",
  "private": true,
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "dev": "convex dev",
    "ship": "convex deploy && pnpm run ship:www && pnpm run ship:app && pnpm run ship:admin",
    "ship:www": "static-hosting upload --prod --component www --dist ../../apps/www/out --no-spa --build-command 'cd ../../apps/www && NEXT_PUBLIC_CONVEX_URL=$VITE_CONVEX_URL pnpm run build'",
    "ship:app": "static-hosting upload --prod --component app --dist ../../apps/app/dist --build-command 'cd ../../apps/app && pnpm run build'",
    "ship:admin": "static-hosting upload --prod --component admin --dist ../../apps/admin/dist --build-command 'cd ../../apps/admin && pnpm run build'",
    "preview:www": "static-hosting upload --component www --dist ../../apps/www/out --no-spa --build-command 'cd ../../apps/www && NEXT_PUBLIC_CONVEX_URL=$VITE_CONVEX_URL pnpm run build'",
    "preview:app": "static-hosting upload --component app --dist ../../apps/app/dist --build-command 'cd ../../apps/app && pnpm run build'",
    "preview:admin": "static-hosting upload --component admin --dist ../../apps/admin/dist --build-command 'cd ../../apps/admin && pnpm run build'",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "auth:setup": "node scripts/setup-auth.mjs",
    "invites:setup": "node scripts/setup-invites.mjs",
    "push:setup": "node scripts/setup-push.mjs",
    "ai:setup": "node scripts/setup-ai.mjs",
    "seed:dev": "convex run --push seed/dev/mutations:apply",
    "seed:dev:code": "node scripts/sign-in-code.mjs"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "catalog:",
    "convex": "catalog:",
    "convex-test": "^0.0.59",
    "typescript": "catalog:",
    "vite": "^8.3.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@auth/core": "^0.41.3",
    "@convex-dev/agent": "^0.7.3",
    "@convex-dev/ai-sdk-provider": "^0.1.0",
    "@convex-dev/auth": "^0.0.95",
    "@convex-dev/static-hosting": "^0.2.1",
    "ai": "^7.0.107"
  }
}
