# Backlog — deliberately deferred

Not scheduled into a phase. Each item was considered and pushed out on purpose; move it into a phase file when it's picked up.

## Email
- **Per-matchmaker email addresses** (`<username>@…`), possibly on a separate domain such as `matchmaker.app` to keep its sending reputation apart from the product domain. The reserved-username list in phase 1 already protects these.
- **Email as a conversation channel:** outbound email to candidates, inbound replies into the thread, reply cleaning, threading headers.
- **Per-matchmaker sending domains** (white-labelled email from the matchmaker's own domain).

## Accounts and profiles
- **Multiple matchmaker profiles in the UI.** The backend already allows it.
- **Closing / deleting a matchmaker profile**, and what its candidates see. Until then, accounts that own a matchmaker profile can't be deleted from the UI.
- **Username moderation:** impersonation of other brands, offensive names.
- **Username changes** (with redirects from the old URL).
- **Team / multi-seat matchmaker accounts.**

## Product
- Billing and pricing.
- Native WhatsApp / Instagram integration.
- Native mobile apps.
- Analytics dashboards.
- Attachments in chat.
- Message editing and deletion.
- Custom domains per matchmaker.
- Cross-matchmaker anything.
